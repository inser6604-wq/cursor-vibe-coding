/**
 * Supabase 설정 예시
 * 1. 이 파일을 config.js로 복사하거나
 * 2. npm run build 로 Vercel 환경변수에서 자동 생성
 */
const SUPABASE_CONFIG = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY',
  tableName: 'reservations'
};
